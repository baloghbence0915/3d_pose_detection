import React from 'react';

import './live-icon.scss';

const LiveIcon = (): JSX.Element => {
    return <span className="live-icon">
        <span className="live-icon-inner"></span>
    </span>
};

export default LiveIcon;
